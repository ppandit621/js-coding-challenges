// Q2. Default Export
// Create greet.js exporting a default function that returns "Hello &lt;name&gt;" . Import it
// as g in main.js .

export default function greet(name) {
  return `Hello ${name}`;
}